Cool task! I definitely would have overlooked descriptors. This time, I started by re-reading some of the questions and checked the theory on copying dates and functions. It also immediately pointed out crucial details, like `null` being an object. So, I basically just ran a recursive loop over the fields. After that, I read the second block about descriptors to figure out how to copy getters and setters since they aren't enumerable. I got a little stuck though because I was getting an array of keys but kept using a `for...in` loop.